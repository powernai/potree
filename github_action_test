This is a dummy file, which you can change in order to trigger a build action.
- Test if it trigger github action to the cpms-web repo
- Editing this to test if it trigger
- This edit is testing if cpms-web will build correctly
- This is testing if the new token is working proeptly 
- This commit is testing if cpms-web only build with develop branch 
- This commit is checking if github action has the potree repo under the directory
- This commit is checking if cpms-web workflow delete the old repo then adds it back
